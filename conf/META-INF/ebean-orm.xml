<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings>
    <entity class="models.Espece">
        <raw-sql name="show">
        	<columnMapping column="cd_nom" property="id" />
            <columnMapping column="nom_complet" property="nomComplet" />
            <columnMapping column="lb_auteur" property="auteur" />
            <columnMapping column="obs_by_grid" property="features" />
            <query>
            	select cd_nom, nom_complet, lb_auteur, famille, phylum, ordre, observations, obs_by_grid from webobs.info_espece where cd_nom = :id
            </query>
        </raw-sql>
    </entity>
    
    <entity class="models.InfoType">
    	<raw-sql name="info">
    		 <columnMapping column="obs_by_grid" property="features" />
    		<query>
    			select ordre, observations, especes, obs_by_grid, observateurs, communes from webobs.info_ordre where ordre = :type
    		</query>
    	</raw-sql>
    </entity>
</entity-mappings>