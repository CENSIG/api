# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /apiv1/:type                controllers.AtlasController.list(type: String)

GET	    /apiv1/:type/:id			controllers.AtlasController.show(type: String, id: Long)
